@model GreenStore.Models.Item

@{
    ViewBag.Title = "Sản phẩm - " + Model.Name;
}

<h2>@Model.Name</h2>

<p>
    <img alt="@Model.Name" src="@Model.ItemPictureUrl" />
</p>

<div id="item-details">
    <p>
        <em>Loại hàng:</em>
        @Model.Catagorie.Name
    </p>
    <p>
        <em>Đơn giá:</em>
        @String.Format("{0:F}", Model.Price)
    </p>
    <p class="button">
        @Html.ActionLink("Thêm vào giỏ hàng", "AddToCart",
        "ShoppingCart", new { id = Model.ID }, "")
    </p>
</div>



